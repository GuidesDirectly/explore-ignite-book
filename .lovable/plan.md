Deploy the `stripe-webhook` edge function to Supabase using `supabase--deploy_edge_functions` with `function_names: ["stripe-webhook"]`. No code or config changes — just a deploy of the already-edited function so the idempotency, error-status, and DB-failure changes go live.