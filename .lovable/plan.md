Update the GA4 Measurement ID in `index.html` from `G-0KGEME028E` to `G-0KGEMEG28E` in both places:

1. The `<script async src="https://www.googletagmanager.com/gtag/js?id=...">` URL.
2. The `gtag('config', '...')` call.

No other files or behavior change.